import { MinimumValuePipe } from './minimum-value.pipe';

describe('MinimumValuePipe', () => {
  it('create an instance', () => {
    const pipe = new MinimumValuePipe();
    expect(pipe).toBeTruthy();
  });
});
