import { ArrowCoordinatesPipe } from './arrow-coordinates.pipe';

describe('ArrowCoordinatesPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrowCoordinatesPipe();
    expect(pipe).toBeTruthy();
  });
});
